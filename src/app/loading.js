import React from 'react';
import { Atom } from 'react-loading-indicators';

const Loading = () => {
    return (
        <div className='flex justify-center'>
            <Atom color="#29dbff" size="large" text="" textColor="" />
        </div>
    );
};

export default Loading;